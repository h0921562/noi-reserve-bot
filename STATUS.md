---
project: NOI/雑務/noi-reserve-bot
status: in_progress
health: at_risk
updated: 2026-07-22
summary: 日時パースと取消フローを修正し53/53の回帰テストを整備。無料枠枯渇の原因は未確定（エラー文面待ち）
next_action: botが返すエラー文面を受領して無料枠の枯渇原因を確定する。あわせてpush→reply移行とcriticレビューの可否を確認する
---

# 会議室予約bot — 進捗と引き継ぎ

## 現在地

- 目的: LINEから日総第２２ビルの会議室（4階共用/6階）を予約・空き確認・取消する
- ホスト: **Render 無料プラン**（`https://noi-reserve-bot.onrender.com`）。GitHub `h0921562/noi-reserve-bot`
- 2026-07-22: 日時パースの重大バグ群と取消フローを修正。**未デプロイ**

## 直近の完了（2026-07-22）

- 回帰フィクスチャ整備（`test/cases.js` 53ケース、`test/run.js`、`test/flow.js`）。`npm test` で実行
- **修正前 26/53 pass → 修正後 53/53 pass**（修正前の実測は `test/result_baseline.json` に保全）
- 日時パーサを `datetime.js` に切り出して全面修正。直した実害:
  - 曜日パーサが「7月22日」の「月」を月曜、「から」の「か」を火曜と誤認し**別の日に予約が入っていた**（7/22→7/27、7/28 等）
  - 範囲区切りの「から」を文字クラス `[-~〜から]` に入れていたため**終了時刻が落ちて常に1時間に丸められていた**
  - 午前/午後、終了側の「半」、所要時間指定（2時間/30分）が未対応だった
  - 日付計算がサーバのTZ依存。`TZ` 未設定なら深夜〜早朝JSTで**1日ずれる**（TZ=UTCで実証）→ JST固定に変更
- 取消フローを修正:
  - 前処理が取消語より前を全削除して**日時ごと消していた**（10パターン中8件が失敗）→ `stripCancelKeyword` でその場除去に変更
  - 複数予約時に「番号を送ってください」と言うのに**受け取る状態が無かった** → `pendingCancels` を追加
  - 期限超過時の確認フローが TODO のまま行き止まりだった → 「はい/いいえ」で確定できるように
  - キャンセル期限の計算がサーバTZ依存だった → JST固定

## 次のアクション

- [ ] P0: **botが返すエラー文面を受領し、無料枠の枯渇原因を確定する**（下記「未確定」参照）
- [ ] P1: 予約フローの push→reply 移行（現状 予約1件で push 2通、グループでは人数倍）
- [ ] P1: critic による独立レビュー（L2該当のため）と品質記録の作成
- [ ] P2: `reserve.js` が操作のたびに `login()` で5往復している無駄の解消（応答が7〜10秒かかる一因）

## 判断・前提

- 決定済み: 日時パースは `datetime.js` に分離しテスト可能にする。基準時刻を引数で注入して決定的にテストする
- 決定済み: 結果は reply で返し、失敗時のみ push にフォールバック（`sendResult`）。取消系は適用済み
- 未確定: **どの無料枠が枯れているか**。候補は3つ
  - Render 750h/月（ワークスペース共有）。[index.js](index.js) の14分セルフpingで本botだけで744h/月を消費する計算
  - LINE 200通/月。push のみ課金・**グループでは人数分カウント**（公式ドキュメントで確認済み）
  - Groq / Anthropic のレート制限（議事録機能のみ使用）
- 未確認: Render の環境変数 `TZ` が `Asia/Tokyo` か。未設定なら旧実装は早朝に日付がずれていた
- 未確認: Render の自動デプロイ（GitHub push で反映されるか）

## リスク・ブロッカー

- **L2案件**: 誤った日時で会議室が実際に押さえられ、開始2時間前を過ぎると課金される（金銭・不可逆）
- セルフpingが Render 無料枠を意図的に食い潰している。停止すればコールドスタート（初回応答が遅れる）とのトレードオフ
- 議事録機能（`minutes.js`）は今回未着手。push を1回あたり5〜6通使っており、LINE枠の最大の消費源

## 再開手順

1. 最初に読む: 本ファイルと [会議室予約自動化_設計メモ.md](../会議室予約自動化_設計メモ.md)
2. テスト: `cd projects/NOI/雑務/noi-reserve-bot && npm test`（53ケース＋5シナリオ）
3. 修正前との比較: `test/result_baseline.json`（26/53）と突き合わせる
4. デプロイ: 未確認。Render の自動デプロイ設定を確認してから

## 関連ファイル

- [index.js](index.js) — LINE webhook・会話フロー
- [datetime.js](datetime.js) — 日時パース（2026-07-22 新設）
- [reserve.js](reserve.js) — sd-reservsys.jp のスクレイピング
- [minutes.js](minutes.js) — 議事録（今回対象外）
- [test/](test/) — 回帰テスト一式

## 更新履歴

- 2026-07-22: 日時パース・取消フローを修正。回帰テスト整備。無料枠の原因特定は未了
